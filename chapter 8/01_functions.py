# functions are used for using the  code again and again without writing the whole code and just by calling the function again


# Function definition
def avg() :
        a = int(input("Enter the number 1 :"))
        b = int(input("Enter the number 2 :"))
        c = int(input("Enter the number 3 :"))
        avergae = (a+b+c)/3
        print(avergae)
        
#Function call
avg() # Now i can call this function many times without writing the whole code again
avg()
avg()
avg()
avg()

        